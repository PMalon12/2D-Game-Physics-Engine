//#pragma once
//#include "followable.h"
//#include "world.h"

/*
void Followable::updateScreenOffset(Viewport* pViewport)
{
screenOffset = (getWorldPosition() - (pViewport->getWorldPosition() / pViewport->getVPZoomOut()));
}
*/